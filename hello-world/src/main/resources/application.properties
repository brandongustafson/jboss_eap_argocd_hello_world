# Application name — shows up in Spring Boot actuator and logs
spring.application.name=hello-world

# Context path when running standalone (java -jar).
# When deployed to JBoss EAP, the context root is controlled by jboss-web.xml instead.
server.servlet.context-path=/

# Port for standalone mode. JBoss EAP ignores this.
server.port=8080
